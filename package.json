{
  "name": "api_videojuegos_postgres",
  "version": "1.0.0",
  "description": "api videojuegos con postgres",
  "license": "ISC",
  "author": "rbp",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file=.env servidor.js",
    "dev": "node --env-file=.env --watch servidor.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "pg": "^8.18.0"
  }
}
